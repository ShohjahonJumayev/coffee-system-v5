services:
  coffee-system:
    build: .
    container_name: coffee-system-v5
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "3000:3000"
    volumes:
      - coffee_data:/app/data

volumes:
  coffee_data:
