const $=id=>document.getElementById(id), money=n=>new Intl.NumberFormat('ru-RU').format(Number(n||0)), esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
let branches=[],products=[],cart=[],category='Все',branchId=Number(new URLSearchParams(location.search).get('branch')||0);
async function get(url,opts){const r=await fetch(url,opts);const d=await r.json();if(!r.ok)throw new Error(d.error||'Ошибка');return d}
async function init(){branches=await get('/api/public/branches');if(!branchId||!branches.some(b=>Number(b.id)===branchId))branchId=Number(branches[0]?.id||0);$('branchSelect').innerHTML=branches.map(b=>`<option value="${b.id}">${esc(b.name)}</option>`).join('');$('branchSelect').value=branchId;$('branchSelect').onchange=()=>{branchId=Number($('branchSelect').value);history.replaceState(null,'',`?branch=${branchId}`);loadProducts()};await loadProducts()}
async function loadProducts(){products=await get('/api/public/products?branch='+branchId);$('branchName').textContent=branches.find(b=>Number(b.id)===branchId)?.name||'Выберите товары';renderCats();renderProducts();renderCart()}
function renderCats(){const cats=['Все',...new Set(products.map(p=>p.category))];$('categories').innerHTML=cats.map(c=>`<button class="chip ${c===category?'active':''}" data-cat="${esc(c)}">${esc(c)}</button>`).join('');document.querySelectorAll('[data-cat]').forEach(b=>b.onclick=()=>{category=b.dataset.cat;renderCats();renderProducts()})}
function emoji(cat){return /дес/i.test(cat)?'🍰':/напит|мох/i.test(cat)?'🥤':'☕'}
function renderProducts(){const arr=category==='Все'?products:products.filter(p=>p.category===category);$('products').innerHTML=arr.length?arr.map(p=>`<div class="menu-item"><div class="emoji">${emoji(p.category)}</div><h3>${esc(p.name)}</h3><small>${esc(p.category)}</small><footer><b>${money(p.price)} сум</b><button data-add="${p.id}">+</button></footer></div>`).join(''):'<div class="empty">Нет товаров</div>';document.querySelectorAll('[data-add]').forEach(b=>b.onclick=()=>add(Number(b.dataset.add)))}
function add(id){const p=products.find(x=>Number(x.id)===id);const f=cart.find(x=>x.productId===id);if(f)f.quantity++;else cart.push({productId:id,name:p.name,price:Number(p.price),quantity:1});renderCart()}
function renderCart(){const total=cart.reduce((s,x)=>s+x.price*x.quantity,0),count=cart.reduce((s,x)=>s+x.quantity,0);$('total').textContent=$('drawerTotal').textContent=money(total);$('count').textContent=count;$('cartList').innerHTML=cart.length?cart.map(x=>`<div class="cart-row"><div><b>${esc(x.name)}</b><br><small>${money(x.price)} сум</small></div><div class="cart-actions"><button onclick="change(${x.productId},-1)">−</button><b>${x.quantity}</b><button onclick="change(${x.productId},1)">+</button></div></div>`).join(''):'<div class="empty">Корзина пуста</div>'}
window.change=(id,d)=>{const x=cart.find(i=>i.productId===id);if(!x)return;x.quantity+=d;if(x.quantity<=0)cart=cart.filter(i=>i.productId!==id);renderCart()};
$('openCart').onclick=()=>$('drawer').classList.remove('hide');$('closeCart').onclick=()=>$('drawer').classList.add('hide');$('drawer').onclick=e=>{if(e.target===$('drawer'))$('drawer').classList.add('hide')};
$('sendOrder').onclick=async()=>{const customerName=$('customerName').value.trim(),customerPhone=$('customerPhone').value.trim();if(!cart.length)return alert('Добавьте товары');if(!customerName||!customerPhone)return alert('Введите имя и телефон');$('sendOrder').disabled=true;try{const d=await get('/api/public/orders',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({branchId,customerName,customerPhone,items:cart.map(({productId,quantity})=>({productId,quantity}))})});$('drawer').classList.add('hide');$('successText').textContent=`Номер заказа: ${d.order.id}. Сумма: ${money(d.order.total)} сум.`;$('success').classList.remove('hide');cart=[];renderCart()}catch(e){alert(e.message)}finally{$('sendOrder').disabled=false}};
init().catch(e=>document.body.innerHTML=`<div class="empty">${esc(e.message)}</div>`);
